import { CuentaDefectosCorregidosPipe } from './cuenta-defectos-corregidos.pipe';

describe('CuentaDefectosCorregidosPipe', () => {
  it('create an instance', () => {
    const pipe = new CuentaDefectosCorregidosPipe();
    expect(pipe).toBeTruthy();
  });
});

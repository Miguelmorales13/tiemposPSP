import { OrdenDefectosPipe } from './orden-defectos.pipe';

describe('OrdenDefectosPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenDefectosPipe();
    expect(pipe).toBeTruthy();
  });
});

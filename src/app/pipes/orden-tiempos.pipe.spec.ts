import { OrdenTiemposPipe } from './orden-tiempos.pipe';

describe('OrdenTiemposPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenTiemposPipe();
    expect(pipe).toBeTruthy();
  });
});

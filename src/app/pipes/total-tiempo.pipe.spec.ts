import { TotalTiempoPipe } from './total-tiempo.pipe';

describe('TotalTiempoPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalTiempoPipe();
    expect(pipe).toBeTruthy();
  });
});
